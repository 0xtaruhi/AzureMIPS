package azuremips.core.ifu

import spinal.core._
import spinal.lib._

import azuremips.core._

case class Btb(
  config: CoreConfig = CoreConfig()
) extends Component {
  val io = new Bundle {

  }
  
}