package azuremips.core.rtu

import spinal.core._
import spinal.lib._

import azuremips.core._

case class Retire(
  config: CoreConfig = CoreConfig()
) extends Component {
  val io = new Bundle {
     
  }
}